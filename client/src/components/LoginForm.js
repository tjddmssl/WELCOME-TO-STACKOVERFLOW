function LoginForm() {
  return (
    <div>
      email, password from <br />
      login button <br />
      {`Don't have an account? sign up (link to signup page)`}
    </div>
  );
}

export default LoginForm;
